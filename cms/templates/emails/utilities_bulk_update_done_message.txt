<%! from django.utils.translation import ugettext as _ %>
${_("Hello, {user}").format(user=user_username)}

% if success:
${_("Your bulk update on the {course} course has successfully completed. Previously published problems have been automatically republished.").format(course=course_name)}
% else:
${_("Your bulk update on the {course} course has failed due to internal error. Problems in your course may be partially updated. Please contact our team for help.").format(course=course_name)}
% endif

${_("The following settings were modified:")}

% for key, value in modified_settings.iteritems():
  ${_("{key}: {value}").format(key=key, value=value)}
% endfor
